name: build-and-push

on:
  push:
    branches:
      - main
    tags:
      - "v*"
    paths:
      - ".github/workflows/build-and-push.yml"
      - "build.gradle"
      - "settings.gradle"
      - "gradle/**"
      - "gradlew"
      - "platform/**"
      - "services/**"
      - "shared/**"
      - "scripts/ci/**"
  workflow_dispatch:
    inputs:
      build_scope:
        description: "affected, all, or comma-separated service image names"
        required: false
        default: "affected"
      image_mode:
        description: "jvm or native"
        required: false
        default: "native"

permissions:
  contents: read

concurrency:
  group: ircs-v4-backend-images-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-arch-images:
    name: build-${{ matrix.arch }}-images
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: amd64
            runner: ubuntu-24.04
          - arch: arm64
            runner: ubuntu-24.04-arm
    env:
      REGISTRY: docker.io/speedproxy
      PUSH_REGISTRY: docker.io/speedproxy
      TARGET_ARCH: ${{ matrix.arch }}
      TARGET_OS: linux
      TARGET_PLATFORMS: linux/${{ matrix.arch }}
      REGISTRY_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      REGISTRY_PASSWORD: ${{ secrets.DOCKERHUB_TOKEN }}
      BUILD_SCOPE_INPUT: ${{ inputs.build_scope || 'affected' }}
      IMAGE_MODE_INPUT: ${{ inputs.image_mode || 'native' }}
      JIB_BASE_IMAGE: eclipse-temurin:25-jre-alpine
      JIB_ALLOW_INSECURE_REGISTRIES: "false"
      JIB_SEND_CREDENTIALS_OVER_HTTP: "false"
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "25"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push images
        shell: bash
        run: |
          set -euo pipefail
          chmod +x gradlew
          chmod +x scripts/ci/jib-build-push-affected.sh
          if [[ "${GITHUB_REF_TYPE}" == "tag" ]]; then
            base_image_tag="${GITHUB_REF_NAME}"
          else
            base_image_tag="sha-$(git rev-parse --short=12 HEAD)"
          fi
          export IMAGE_TAG="${base_image_tag}-${TARGET_ARCH}"
          export BUILD_SCOPE="${BUILD_SCOPE_INPUT:-affected}"
          export IMAGE_MODE="${IMAGE_MODE_INPUT:-native}"
          scripts/ci/jib-build-push-affected.sh

      - name: Upload image manifest
        uses: actions/upload-artifact@v4
        with:
          name: build-image-manifest-${{ matrix.arch }}
          path: build-image-manifest.txt
          if-no-files-found: error

      - name: Print image manifest
        shell: bash
        run: cat build-image-manifest.txt

  publish-multiarch-images:
    name: publish-multiarch-images
    runs-on: ubuntu-24.04
    needs: build-arch-images
    env:
      REGISTRY: docker.io/speedproxy
      PUSH_REGISTRY: docker.io/speedproxy
      PROD_CONFIG_REPO_BRANCH: main
      PROD_K8S_DIRS: prod-config-repo/ircs-v4
      IMAGE_ARCHES: amd64 arm64
      REGISTRY_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      REGISTRY_PASSWORD: ${{ secrets.DOCKERHUB_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Download arch manifests
        uses: actions/download-artifact@v4
        with:
          path: build-image-manifests
          pattern: build-image-manifest-*

      - name: Publish multi-arch image manifests
        shell: bash
        run: |
          set -euo pipefail
          chmod +x scripts/ci/publish-multiarch-manifests.sh
          if [[ "${GITHUB_REF_TYPE}" == "tag" ]]; then
            export IMAGE_TAG="${GITHUB_REF_NAME}"
          else
            export IMAGE_TAG="sha-$(git rev-parse --short=12 HEAD)"
          fi
          export ARTIFACT_DIR="build-image-manifests"
          scripts/ci/publish-multiarch-manifests.sh

      - name: Configure GitOps deploy key
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p ~/.ssh
          printf '%s\n' "${{ secrets.GITOPS_DEPLOY_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan github.com >> ~/.ssh/known_hosts

      - name: Update production config image tags
        shell: bash
        run: |
          set -euo pipefail
          if [[ "${GITHUB_REF_TYPE}" == "tag" ]]; then
            echo "Tag build detected; production manifests are not rewritten from release tags."
            exit 0
          fi
          if [[ ! -s ~/.ssh/id_ed25519 ]]; then
            echo "GITOPS_DEPLOY_KEY is required to update production manifests." >&2
            exit 2
          fi
          git clone \
            --depth 1 \
            --branch "${PROD_CONFIG_REPO_BRANCH}" \
            "git@github.com:Prodigalgal/ircs-prod-config.git" \
            prod-config-repo

          chmod +x scripts/ci/update-prod-image-tags.sh
          scripts/ci/update-prod-image-tags.sh build-image-manifest.txt

          cd prod-config-repo
          if git diff --quiet -- ircs-v4; then
            echo "No production manifest image changes."
            exit 0
          fi
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add ircs-v4
          git commit -m "ci: update ircs v4 image tags"
          git push origin "HEAD:${PROD_CONFIG_REPO_BRANCH}"

      - name: Print image manifest
        shell: bash
        run: cat build-image-manifest.txt
